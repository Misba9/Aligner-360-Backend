# Database configuration
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?<options>
DATABASE_URL=mongodb+srv://ahmedmisba405_db_user:oZoqb0gNPIzyNnQ2@cluster0.evr8cdw.mongodb.net/orthodontist_portal?retryWrites=true&w=majority

# JWT configuration
# Used for signing and verifying JWT tokens for authentication
# Generate a strong secret using: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here

# Admin emails (comma-separated list of emails that should be admins)
ADMIN_EMAILS=admin@example.com,superadmin@example.com

# Application URLs
ADMIN_PANEL_URL=http://localhost:3001
FRONTEND_URL=http://localhost:3000

# ImageKit configuration (required by the app for image uploads)
# Sign up at https://imagekit.io/ to get these credentials
IMAGEKIT_PUBLIC_KEY=your_imagekit_public_key
IMAGEKIT_PRIVATE_KEY=your_imagekit_private_key
IMAGEKIT_URL_ENDPOINT=your_imagekit_url_endpoint

# Port configuration
PORT=8080